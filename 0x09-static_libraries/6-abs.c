#include "main.h"
/**
 * _abs - absolute value of an integer
 *
 * @n: integer to make absolute
 * Return: absolute value of integer
 *
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
